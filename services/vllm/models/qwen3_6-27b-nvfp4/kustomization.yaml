apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: inference-club
resources:
  - ../../base
patches:
  - path: deployment.yaml
    target:
      kind: Deployment
      name: spark-llm
  - path: service.yaml
    target:
      kind: Service
      name: spark-llm
