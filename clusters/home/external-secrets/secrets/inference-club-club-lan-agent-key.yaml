apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: club-lan-agent-key
  namespace: inference-club
spec:
  refreshInterval: 1h
  secretStoreRef: { name: vaultwarden, kind: ClusterSecretStore }
  target:
    name: club-lan-agent-key
    creationPolicy: Owner
  data:
    - secretKey: api-key
      remoteRef: { key: inference-club-club-lan-agent-key, property: api-key }

