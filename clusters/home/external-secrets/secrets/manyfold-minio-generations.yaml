apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: minio-generations
  namespace: manyfold
spec:
  refreshInterval: 1h
  secretStoreRef: { name: vaultwarden, kind: ClusterSecretStore }
  target:
    name: minio-generations
    creationPolicy: Owner
  data:
    - secretKey: access-key
      remoteRef: { key: manyfold-minio-generations, property: access-key }
    - secretKey: secret-key
      remoteRef: { key: manyfold-minio-generations, property: secret-key }

