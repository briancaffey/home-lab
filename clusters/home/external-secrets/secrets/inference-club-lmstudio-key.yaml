apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: lmstudio-key
  namespace: inference-club
spec:
  refreshInterval: 1h
  secretStoreRef: { name: vaultwarden, kind: ClusterSecretStore }
  target:
    name: lmstudio-key
    creationPolicy: Owner
  data:
    - secretKey: api-key
      remoteRef: { key: inference-club-lmstudio-key, property: api-key }

