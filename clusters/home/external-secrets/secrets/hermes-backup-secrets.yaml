apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: backup-secrets
  namespace: hermes
spec:
  refreshInterval: 1h
  secretStoreRef: { name: vaultwarden, kind: ClusterSecretStore }
  target:
    name: backup-secrets
    creationPolicy: Owner
  data:
    - secretKey: restic-password
      remoteRef: { key: hermes-backup-secrets, property: restic-password }
    - secretKey: s3-access-key
      remoteRef: { key: hermes-backup-secrets, property: s3-access-key }
    - secretKey: s3-secret-key
      remoteRef: { key: hermes-backup-secrets, property: s3-secret-key }

