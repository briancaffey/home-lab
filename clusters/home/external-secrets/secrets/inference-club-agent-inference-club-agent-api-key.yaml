apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: agent-inference-club-agent-api-key
  namespace: inference-club
spec:
  refreshInterval: 1h
  secretStoreRef: { name: vaultwarden, kind: ClusterSecretStore }
  target:
    name: agent-inference-club-agent-api-key
    creationPolicy: Owner
  data:
    - secretKey: api-key
      remoteRef: { key: inference-club-agent-inference-club-agent-api-key, property: api-key }

