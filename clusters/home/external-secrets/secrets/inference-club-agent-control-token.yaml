apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: agent-control-token
  namespace: inference-club
spec:
  refreshInterval: 1h
  secretStoreRef: { name: vaultwarden, kind: ClusterSecretStore }
  target:
    name: agent-control-token
    creationPolicy: Owner
  data:
    - secretKey: token
      remoteRef: { key: inference-club-agent-control-token, property: token }

