apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ngc-api-key
  namespace: inference-club
spec:
  refreshInterval: 1h
  secretStoreRef: { name: vaultwarden, kind: ClusterSecretStore }
  target:
    name: ngc-api-key
    creationPolicy: Owner
  data:
    - secretKey: api-key
      remoteRef: { key: inference-club-ngc-api-key, property: api-key }

