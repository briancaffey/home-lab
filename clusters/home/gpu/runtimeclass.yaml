# k3s auto-detects the nvidia container runtime (already installed for docker
# on every GPU box) and registers it with its embedded containerd; this
# RuntimeClass lets pods select it.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
