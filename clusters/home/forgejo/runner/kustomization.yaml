apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: forgejo

resources:
  - storage.yaml
  - deployment.yaml

configMapGenerator:
  - name: forgejo-runner-config
    files:
      - config.yml=config.yaml
