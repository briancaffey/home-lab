apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: renovate

# Apply with: kubectl apply -k clusters/home/renovate
# Prereqs (out of band): renovate-secrets secret (see cronjob.yaml header),
# mkcert-ca ConfigMap (scripts/lan-certs.sh).
resources:
  - namespace.yaml
  - cronjob.yaml
