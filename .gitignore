# Never commit secrets to this GitOps repo — create them in-cluster instead.
*.env
.env*
*.key
*.pem
*.crt
*.p12
*.pfx
id_rsa*
*kubeconfig*
kubeconfig
*.tfstate
*.tfstate.*
secret*.yaml
secrets*.yaml
*-secret.yaml
*.secret.yaml

# kustomize `--enable-helm` pulls charts here on build; re-pulled each time,
# so we don't vendor them.
charts/
